animation MaxDamageShow
{
    parallel
    {
        ChangeProperty("./", "UIAttachLayoutSlaveComponent", "additionalOffset", vec2(271, 0), 0);
        Opacity("**/PopupContent", 0, 0);
    }

    parallel
    {
        ChangeProperty("./", "UIAttachLayoutSlaveComponent", "additionalOffset", vec2(231, 0), 0.4, SINE_OUT);
        Opacity("**/PopupContent", 1, 0.4, SINE_OUT);
    }
}