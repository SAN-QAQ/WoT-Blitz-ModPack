inline float F_Shlick(float NdotV, float Cspec)
{
	return lerp(Cspec, 1.0, pow5Exp(NdotV));
}

inline float3 F_ShlickVec3(float NdotV, float3 Cspec)
{
	return lerp(Cspec, const1List3, pow5Exp(NdotV));
}
